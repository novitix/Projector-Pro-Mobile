<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:iOS="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
             iOS:Page.UseSafeArea="True"
             xmlns:cc="clr-namespace:CustomControls"
             x:Class="ProjectorProMobile.Pages.PageSearch">
    <ContentPage.Resources>
        <ResourceDictionary>
            <LinearGradientBrush x:Key="ButtonBackground" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#b8c5d6" Offset="0"/>
                <GradientStop Color="#a39ba8" Offset="1"/>
            </LinearGradientBrush>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout>
            <!-- Place new controls here -->
            <cc:ModernEntry x:Name="txtSearch" HeightRequest="50" ReturnType="Search" Placeholder="Search" FontSize="20" TextChanged="Search_TextChanged" Margin="20,10,20,0" BackgroundColor="{AppThemeBinding Light=#efefef, Dark=#222222}">
                <cc:ModernEntry.Keyboard>
                    <Keyboard x:FactoryMethod="Create">
                        <x:Arguments>
                            <KeyboardFlags>
                                None
                            </KeyboardFlags>
                        </x:Arguments>
                    </Keyboard>
                </cc:ModernEntry.Keyboard>
            </cc:ModernEntry>


            <Grid>
                <ListView SelectionMode="None" x:Name="ListResultsView" ItemTapped="ListResultsView_ItemTapped">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextCell Text="{Binding}" TextColor="{AppThemeBinding Light=#03256C, Dark=White}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Label x:Name="lblMessage" HorizontalOptions="Center" Padding="25, 20, 25, 0"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>