<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:iOS="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
             iOS:Page.UseSafeArea="True"
             xmlns:cc="clr-namespace:CustomControls"
             x:Class="ProjectorProMobile.Pages.PageDisplay">

    <ContentPage.Resources>
        
    </ContentPage.Resources>
    
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <BoxView BackgroundColor="{DynamicResource LyricsBgColor}" Grid.Column="0" Grid.Row="0"/>
            <ScrollView x:Name="scrollVw" Orientation="Vertical" VerticalOptions="StartAndExpand" Grid.Column="0" Grid.Row="0" IsClippedToBounds="True">
                <Label x:Name="txtContent" VerticalOptions="FillAndExpand" Text="{Binding Body}" TextColor="{DynamicResource LyricsFgColor}" FontSize="28" Margin="30" FontAttributes="Bold" LineHeight="1.3" LineBreakMode="WordWrap"/>
            </ScrollView>
            <ImageButton x:Name="btnExitDisplay" Source="{DynamicResource ExitIconName}" WidthRequest="50" HeightRequest="50" CornerRadius="25"
                         Padding="11" Margin="8" BackgroundColor="{DynamicResource ControlBgColor}"
                         Clicked="btnExitDisplay_Clicked" HorizontalOptions="EndAndExpand" VerticalOptions="EndAndExpand"/>
        </Grid>
            
    </ContentPage.Content>
</ContentPage>